1.Connect to MongoDB
  Install MongoDB and Mongoose
  setup the MVC folder structure
  config/db.js :-Connect to MongoDB : create config/db.js file to manage the MongoDB connection

2.Create the ticket model
  Define the schema for lottery ticket in models/Ticket.js

3.Create the lottery controller
  Add logic to handle request in controller/lotteryController.js

4.Define Lottery Routes
  set up Routes in routes/lotteryRoutes.js  
  

